Recibido. Queda afinado así:

- No migrar `nurseName` a `nurseId` dentro de `V1.1 Sprint 1` salvo choque real.
- `PaymentRecord` queda como concepto futuro; cobros siguen derivados de `Shift`.
- `allowBackup=true` se registra como deuda sensible, no bloqueante.
- No hacer limpieza global de textos; solo mover a `strings.xml` lo nuevo o lo cercano a pantallas tocadas.
- La verificación fallida en mi shell es de entorno JDK, no de estado del proyecto.
- La prioridad inmediata queda cerrada: **editar enfermeras + `NurseFormViewModelTest`**, después guardias.

Cuando me pidas programar, arrancaría exactamente por edición de enfermeras de punta a punta, siguiendo el patrón ya existente de pacientes.